[package]
name = "diesel_demo"
version = "0.1.0"
authors = ["Sai Wolf <saiwolf@swmnu.net>"]

[dependencies]
diesel = { version = "0.15.0", features = ["sqlite"] }
diesel_codegen = { version = "0.15.0", features = ["sqlite"] }
dotenv = "0.9.0"
